{
  "id": "019d4c19-c598-7391-b6d8-bec90b4194a3",
  "content": "et sRGB, same shape\n    pub target: Vec<[f32; 3]>,\n    /// Depth map, values in [0.0, 1.0], same shape\n    pub depth: Vec<f32>,\n    /// Per-pixel importance weights (pre-computed)\n    pub importance: Vec<f32>,\n    pub width: usize,\n    pub height: usize,\n}\npub fn compute_importance(depth: &[f32], width: usize, height: usize) -> Vec<f32> {\n    let n = width * height;\n    assert_eq!(depth.len(), n);\n\n    // --- Sobel edge detection ---\n    let mut edge_mag = vec![0.0_f32; n];\n    for y in 0..height {\n        for x in 0..width {\n            // 3×3 Sobel kernels\n            // dx: [[-1,0,1],[-2,0,2],[-1,0,1]]\n            // dy: [[-1,-2,-1],[0,0,0],[1,2,1]]\n            let mut dx = 0.0_f32;\n            let mut dy = 0.0_f32;\n            for ky in -1_i32..=1 {\n                for kx in -1_i32..=1 {\n                    let ny = (y as i32 + ky).clamp(0, height as i32 - 1) as usize;\n                    let nx = (x as i32 + kx).clamp(0, width as i32 - 1) as usize;\n                    let v = depth[ny * width + nx];\n                    let kx_w = kx as f32;\n                    let ky_w = ky as f32;\n                    // Sobel X: weight = kx * (2 - |ky|)\n                    let wx = kx_w * (2.0 - ky_w.abs());\n                    let wy = ky_w * (2.0 - kx_w.abs());\n                    dx += v * wx;\n                    dy += v * wy;\n                }\n            }\n            edge_mag[y * width + x] = (dx * dx + dy * dy).sqrt();\n        }\n    }\n\n    // --- Gaussian blur σ=8 on edge magnitude (separated 1D) ---\n    let sigma = 8.0_f32;\n    let radius = (3.0 * sigma).ceil() as usize; // = 24\n    // Build 1D kernel\n    let kernel_len = 2 * radius + 1;\n    let mut kernel = vec![0.0_f32; kernel_len];\n    let mut kernel_sum = 0.0_f32;\n    for (i, slot) in kernel.iter_mut().enumerate() {\n        let offset = i as f32 - radius as f32;\n        let v = (-offset * offset / (2.0 * sigma * sigma)).exp();\n        *slot = v;\n        kernel_sum += v;\n    }\n    for k in kernel.iter_mut() {\n        *k /= kernel_sum;\n    }",
  "source_version": "crates/dorea-lut/src/build.rs",
  "scope_id": "dorea",
  "workstream": "pipeline",
  "recorded_at": "2026-04-02T02:50:50.648494701Z",
  "valid_from": "2026-04-02T02:50:50.648494701Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/dorea-lut/src/build.rs",
    "language": "rs",
    "chunk_type": "function_item",
    "start_line": 30,
    "end_line": 76,
    "content_role": "code",
    "source_origin": "repo:dorea"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "episodic",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}