{
  "id": "019d6b22-2630-7733-9a21-cf4de5b43588",
  "content": "marray(img), max_size)\n            wh_set.add((tw, th))\n            out_w, out_h = tw, th\n            tensors.append(normalize(transforms.ToTensor()(r)))\n\n        if len(wh_set) > 1:\n            # Different post-resize dimensions: stacking is not possible.\n            # Raise a clear error — the fused GPU path requires uniform input dims.\n            raise ValueError(\n                f\"infer_batch_gpu: images have different post-resize dimensions {wh_set}. \"\n                \"The GPU tensor path requires uniform dimensions. Use infer_batch() for \"\n                \"mixed-size inputs or ensure all images have the same source resolution.\"\n            )\n\n        batch = torch.stack(tensors)\n        if self.device.type == \"cuda\":\n            batch = batch.pin_memory().to(self.device, non_blocking=True)\n        else:\n            batch = batch.to(self.device)\n\n        with torch.no_grad():\n            out = self.model(batch)  # (N, 3, H, W) in [-1, 1]\n\n        out = ((out + 1.0) / 2.0).clamp(0.0, 1.0)  # [0, 1], still on device\n\n        # RAUNE-Net (U-Net) may pad internally and produce output dims that differ\n        # from input dims. Force back to the expected (out_h, out_w) so the PNG\n        # we encode matches the enhanced_width/enhanced_height we report in JSON.\n        if out.shape[2] != out_h or out.shape[3] != out_w:\n            import torch.nn.functional as F\n            out = F.interpolate(out, size=(out_h, out_w), mode=\"bilinear\", align_corners=False)\n\n        return out, out_w, out_h",
  "source_version": "python/dorea_inference/raune_net.py",
  "scope_id": "dorea",
  "workstream": "pipeline",
  "recorded_at": "2026-04-08T03:28:13.360644899Z",
  "valid_from": "2026-04-08T03:28:13.360644899Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "python/dorea_inference/raune_net.py",
    "language": "py",
    "chunk_type": "function_definition",
    "start_line": 207,
    "end_line": 232,
    "content_role": "code",
    "source_origin": "repo:dorea"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "episodic",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}